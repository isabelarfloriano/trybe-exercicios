// 1 - 
// Faça um algoritmo que calcule a soma de 1 a 50 usando a estrutura "for" e retorne no formato:
//A soma total de 1 a 50 é:

/* let numbers = 0
for (let index = 0; index <= 50; index +=1) {
    numbers = numbers + index
}
console.log(numbers)
 */

//2 -
// Crie um algoritmo que conte quantos números do intervalo entre 2 e 150 são divisíveis por 3.
// Caso a quantidade seja igual a 50, exiba uma mensagem secreta.

/* let numbers = 0
for (let index = 2; index <= 150; index +=1) {
    if (index%3===0) {
        numbers++
    }
}
if (numbers===50) {
    console.log("mensg secreta!")
}
console.log(numbers) */

// 3 -
//Crie um algoritmo que simula o jogo "pedra, papel e tesoura" levando em consideração que são apenas duas pessoas jogando e imprima o resultado no formato:
// "Player 1 won" ou "A Ties" ou "Player 2 won". 
let player1 
let player2
let game = ["pedra", "papel", "tesoura"]
//4
//Desenvolva um algoritmo que verifica se a pessoa é maior ou menor de idade.
//Imprima no console seguindo o exemplo: "A pessoa é maior de idade".
//Bônus: Crie a condição utilizando operador ternário.
// 5 

//Crie um algoritmo que recebe a idade de Carolzita , Murilo e Baêta e imprime quem é a pessoa mais nova no formato:
// "Pessoa" é a mais nova.

//>>>>>Exercícios do Course:<<<<<
//1-crie um algoritmo que retorne o fatorial de 10.
/* let factorial = 1
for (let index = 2;index<=10;index++) {
    factorial=factorial*index
}
console.log(factorial) */
//2-Desenvolva um algoritmo que é capaz de inverter uma palavra.
/* let word = "tryber";
let word_changed = [];
for (let index=0;index < word.length; index++) {
    word_changed += word[word.length - 1 - index]
}
//ou for (let index = palavra.length - 1; index >=0; index -= 1) for decrescente//

console.log(word_changed) */
//3-Escreva dois algoritmos: um que retorne a maior palavra deste array e outro que retorne a menor.
let array = ['java', 'javascript', 'python', 'html', 'css'];
let cont = ""
/* for (let index=0; index < array.length; index++) {
    if (cont.length<array[index].length) {   ///palavra maior
        cont = array[index]
    } 
} */
for 
console.log(cont)